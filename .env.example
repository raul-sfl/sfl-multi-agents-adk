# ── AI model ─────────────────────────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-lite

# ── Vertex AI (optional) ──────────────────────────────────────────────────────
# Set to 'true' to use Vertex AI instead of AI Studio
# GOOGLE_GENAI_USE_VERTEXAI=true
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1

# ── HelpCenter agent (Dialogflow CX) ─────────────────────────────────────────
# HELP_CENTER_AGENT_ID=projects/PROJECT/locations/LOCATION/agents/AGENT_ID

# ── Conversation logging (Firestore) ─────────────────────────────────────────
# Requires GOOGLE_CLOUD_PROJECT to be set (shares ADC with Vertex AI)
# FIRESTORE_ENABLED=true
# FIRESTORE_COLLECTION_PREFIX=          # e.g. "staging_" for non-prod environments
# HISTORY_RECOVERY_HOURS=48             # how long to look back for previous session

# ── Admin dashboard ───────────────────────────────────────────────────────────
# Access at /admin?key=YOUR_KEY  (leave empty to open in dev — set one in prod!)
# ADMIN_API_KEY=change-me-in-production
